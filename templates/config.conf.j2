#{{ ansible_managed }}

{% for v in nginx_configs[item.key] %}
   {{ v.replace(";",";\n      ").replace("{","{\n      ").replace("}","\n   }\n") }}{% if v.find('{') == -1%} ;{% endif %} 
{% endfor %}
